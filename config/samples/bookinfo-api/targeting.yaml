apiVersion: remesh.bevyx.com/v1alpha1
kind: Targeting
metadata:
  name: test-user-john
spec:
  entrypoint: bookinfo
  priority: 4
  segment:
    httpMatch:
    - headers:
        cookie:
          regex: "^(.*?;)?(user=john)(;.*)?$"
  virtualEnvironment: bookinfo-api